services:
  web:
    image: nginx:1.27-alpine
    container_name: game-design-school-web
    ports:
      - "${WEB_PORT:-8080}:80"
    volumes:
      - .:/srv/materiale:ro
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
